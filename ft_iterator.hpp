#ifndef FT_ITERATOR
# define FT_ITERATOR

#
namespace ft {
	template<typename T, class Allocator = std::allocator<T>>
	class normal_iterator {

	};
	
	template<typename T, class Allocator = std::allocator<T>>
	class reverse_iterator {

	};
}
#endif